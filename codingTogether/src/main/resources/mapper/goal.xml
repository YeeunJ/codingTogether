<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goal">
	<select id="userGoalList" resultType="com.walab.coding.Model.GoalDTO" parameterType="hashmap">
		SELECT goalNum,goal,startDate,endDate FROM goal WHERE userID=#{userID} ORDER BY DATE(startDate) DESC
	</select>
	
	<select id="userGoalList" resultType="com.walab.coding.Model.GoalDTO" parameterType="hashmap">
		SELECT goalNum,goal,startDate,endDate FROM goal WHERE userID=#{userID} ORDER BY DATE(startDate) DESC limit 1
	</select>
</mapper>